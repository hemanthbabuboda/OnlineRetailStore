server:
  port: 9999

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: /Users/bo20426122/Documents/Online_retail_store/ConfigServer/src/main/resources/config
    discovery:
      client:
        service-url:
          defaultZone: http://localhost:8761/eureka/
